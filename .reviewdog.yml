runner:
  actionlint:
    cmd: actionlint
    errorformat:
      - "%f:%l:%c: %m"
    level: error

  shellcheck:
    cmd: shellcheck -f checkstyle .husky/pre-commit .husky/commit-msg .husky/pre-push
    format: checkstyle
    level: warning
