runner:
  actionlint:
    cmd: actionlint
    errorformat:
      - "%f:%l:%c: %m"
    level: error

  shellcheck:
    cmd: shellcheck -f checkstyle .husky/pre-commit .husky/commit-msg
    format: checkstyle
    level: warning
